# kvd single-process test configuration (v1)
#
# This repo's v1 server runs an in-process deterministic Raft cluster (5 static
# voting nodes) and exposes the unary gRPC API defined in proto/kvstore/v1/kv.proto.
#
# Example:
#   ./build/src/kvd --listen_addr=127.0.0.1:50051 --data_dir=./data
